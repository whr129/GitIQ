# Persistent vector store backend powered by ChromaDB
chromadb>=0.4.24

# Default embedding backend (supports CPU installs by default)
sentence-transformers>=2.2.2

# Optional: enable OpenAI embeddings via `repo_rag --backend openai`
openai>=1.30.1

# Compatibility shim for loading TOML configs on Python < 3.11
tomli>=2.0.1; python_version < "3.11"

# API + serverless
fastapi>=0.111.0
uvicorn>=0.29.0
mangum>=0.17.0

# Metadata + state
psycopg[binary]>=3.1.9
redis>=5.0.0
